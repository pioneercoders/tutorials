1. What is CSS and why is it used?
A: CSS (Cascading Style Sheets) is used to style HTML elements — controlling layout, colors, fonts, spacing, and responsiveness. It separates content (HTML) from presentation, improving maintainability.
